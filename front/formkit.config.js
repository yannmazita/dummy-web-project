import { fr } from '@formkit/i18n'
import { genesisIcons } from '@formkit/icons'

const config = {
    icons: {
        genesisIcons,
    },
    locales: { fr },
    locale: 'fr',
}

export default config
